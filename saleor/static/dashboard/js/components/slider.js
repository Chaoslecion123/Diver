/*
* Handler for slider type change event. Manages showing options
* that are connected to selected slider type and hiding others.
*/

/*
function onSliderTypeChange (e) {
  const $target = $(e.currentTarget);
  $('.slider-form').each((index, form) => {
    const $form = $(form);
    $form.toggleClass('hide', $form.data('type') !== $target.val());
  });
}

$('[name="type"]')
  .on('change', onSliderTypeChange)
  .trigger('change');
*/
